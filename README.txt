The HTML files are located in the render folder as well as the primary javascript file. The CSS file contains the CSS and the font file.

To use the desktop application without downloading anything, simply open the renderer folder and open one of the HTML pages.

{ Calendar Page }
Contains the add event button on the top left and different views on the top right

{ Notes Page }